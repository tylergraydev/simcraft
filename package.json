{
  "name": "simhammer",
  "version": "1.5.0",
  "private": true,
  "scripts": {
    "web": "docker compose up --build",
    "desktop:dev": "cd desktop && npm run dev",
    "desktop:build": "cd desktop && npm run build",
    "desktop:build:linux": "docker compose --profile build-linux up build-linux --build",
    "desktop:publish": "cd desktop && npm run publish",
    "kill": "node scripts/kill-ports.js"
  }
}
